application:
  profiles:
    active: local

spring:
  application:
    name: '@project.artifactId@'
  cloud:
    vault:
      enabled: false
      kv:
        application-name: ${spring.application.name}
